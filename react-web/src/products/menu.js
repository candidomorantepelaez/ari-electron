import React from "react";
import { FormattedMessage } from 'react-intl';

const menu = [
  {
    label: <FormattedMessage id="product.menu.label" />,
    to: "/products"
  }
];

export default menu;
